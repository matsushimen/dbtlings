name: 'dbtlings_05'
version: '1.0.0'
config-version: 2

profile: 'dbtlings'

model-paths: ["models"]
seed-paths: ["data"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbtlings_05:
    materialized: view
