name: 'dbtlings_06'
version: '1.0.0'
config-version: 2

profile: 'dbtlings'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
seed-paths: ["data"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbtlings_06:
    materialized: view
