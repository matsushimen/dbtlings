name: 'dbtlings_07'
version: '1.0.0'
config-version: 2

profile: 'dbtlings'

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["data"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbtlings_07:
    materialized: view
