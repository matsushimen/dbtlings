name: 'dbtlings_08'
version: '1.0.0'
config-version: 2

profile: 'dbtlings'

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["data"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbtlings_08:
    materialized: view
